import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MAKAH HR Application',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
